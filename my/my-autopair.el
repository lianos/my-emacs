(autopair-global-mode)

;; Provides functionality wraps selected regions with a pair. This
;; is experimental (from 0.3), but trying to use this instead of the
;; wrap-region package (experimental)
(setq autopair-autowrap t) 

(provide 'my-autopair)
